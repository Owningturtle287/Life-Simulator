{
  "name": "life-simulator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A touch-first artificial chemistry and evolution laboratory",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "start": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/engine.js && node --check src/main.js && node --check src/renderer.js && node --check src/builder.js && node --check src/worker.js"
  },
  "engines": { "node": ">=22" }
}
